---
import '@/styles/global.css';
import Navigation from '@/components/Navigation';
import LanguageToggle from '@/components/LanguageToggle';
import ThemeToggle from '@/components/ThemeToggle';
import AccessibilityPanel from '@/components/AccessibilityPanel';
import CookieBanner from '@/components/CookieBanner';

const { title = 'Meine Website 2026' } = Astro.props;
const ssrLocale = Astro.url.pathname.startsWith('/en') ? 'en' : 'de';
---

<html lang={ssrLocale}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="astro-view-transitions" content="true" />
    <title>{title}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/opendyslexic@1.0.0/css/opendyslexic.min.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=OpenDyslexic" />
    <link href="https://fonts.googleapis.com/css2?family=Satoshi:wght@500;700;900&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Theme-Script: verhindert Flackern beim Laden -->
    <script is:inline>
      (function() {
        var t = localStorage.getItem('theme') || 'system';
        var dark = t === 'dark' || (t === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches);
        if (dark) document.documentElement.classList.add('dark');
      })();
    </script>
  </head>

  <body class="bg-background text-foreground antialiased min-h-screen flex flex-col">
    <!-- MOBILE HEADER ONLY - Desktop bleibt 100% unveraendert -->
    <header class="md:hidden bg-background/95 backdrop-blur-xl sticky top-0 z-50 border-b-0 transition-all duration-300" data-scroll="up">
      <div class="px-6 py-6 flex items-center justify-between">
        <a href={ssrLocale === 'en' ? '/en/' : '/'} class="text-[20px] font-semibold tracking-[-0.015em] text-foreground">
          Anika Warncke
        </a>

        <div class="flex items-center gap-x-5">
          <LanguageToggle client:only="react" />
          <Navigation client:only="react" />
        </div>
      </div>
    </header>

    <!-- Sticky Header Desktop -->
    <header class="hidden md:block bg-background/95 backdrop-blur-xl sticky top-0 z-50" data-scroll="up">
      <div class="max-w-7xl mx-auto px-8 py-9 flex items-center justify-between">
        <a href={ssrLocale === 'en' ? '/en/' : '/'} class="text-[28px] font-semibold tracking-[-0.025em] text-foreground">Anika Warncke</a>

        <nav class="flex items-center gap-x-10 text-lg font-medium">
          <a href={ssrLocale === 'en' ? '/en' : '/'} class="nav-link hover:text-primary transition-colors">Home</a>
          <a href={ssrLocale === 'en' ? '/en/projekte' : '/projekte'} class="nav-link hover:text-primary transition-colors">Projekte</a>
          <a href={ssrLocale === 'en' ? '/en/ueber-mich' : '/ueber-mich'} class="nav-link hover:text-primary transition-colors">Über mich</a>
          <a href={ssrLocale === 'en' ? '/en/contact' : '/kontakt'} class="nav-link hover:text-primary transition-colors">Kontakt</a>
        </nav>

        <div class="flex items-center gap-x-6">
          <LanguageToggle client:only="react" />
          <ThemeToggle client:only="react" />
        </div>
      </div>
    </header>

      <script is:inline>
        let lastScroll = 0;
        window.addEventListener('scroll', () => {
          const headers = document.querySelectorAll('header[data-scroll="up"]');
          const currentScroll = window.scrollY;

          headers.forEach((header) => {
            if (currentScroll > lastScroll && currentScroll > 100) {
              header.classList.add('translate-y-[-100%]');
            } else {
              header.classList.remove('translate-y-[-100%]');
            }
            header.classList.toggle('shadow-md', currentScroll > 10);
          });

          lastScroll = currentScroll;
        });
      </script>

    <!-- Haupt-Content: w-full ensures mobile-first full bleed -->
    <main class="flex-1 w-full">
      <slot />
    </main>

    <!-- === FOOTER – nur Mobile optimiert (Desktop bleibt 100% unverändert) === -->
    <footer class="bg-background border-t border-border/50 pt-10 pb-8 md:pt-16 md:pb-12">
      <div class="max-w-7xl mx-auto px-6 md:px-8">

        <!-- Mobile: kompakt & luftig -->
        <div class="md:hidden space-y-8">
          <!-- Links -->
          <div class="flex flex-wrap justify-center gap-x-8 gap-y-4 text-base font-medium text-foreground">
            <a href="/ueber-mich" class="hover:text-primary transition-colors">Über mich</a>
            <a href="/projekte" class="hover:text-primary transition-colors">Projekte</a>
            <a href="/kontakt" class="hover:text-primary transition-colors">Kontakt</a>
          </div>

          <!-- Legal + Copyright -->
          <div class="text-center text-xs text-muted-foreground space-y-1">
            <div class="flex justify-center gap-6">
              <a href="/impressum" class="hover:text-foreground transition-colors">Impressum</a>
              <a href="/datenschutz" class="hover:text-foreground transition-colors">Datenschutz</a>
            </div>
            <p>© 2026 Anika Warncke • Alle Rechte vorbehalten</p>
          </div>
        </div>

        <!-- Desktop bleibt exakt wie vorher (nichts verändert) -->
        <div class="hidden md:block">
          <div class="container mx-auto max-w-5xl px-6">
            <div class="flex flex-col md:flex-row justify-between items-center gap-8 text-sm">
              <div class="flex flex-wrap gap-x-8 gap-y-2 text-muted-foreground">
                <a href="/ueber-mich" class="hover:text-foreground transition-colors">Über mich</a>
                <a href="/projekte" class="hover:text-foreground transition-colors">Projekte</a>
                <a href="/kontakt" class="hover:text-foreground transition-colors">Kontakt</a>
              </div>

              <div class="flex items-center gap-8 text-muted-foreground">
                <a href="https://www.linkedin.com/in/anika-warncke" target="_blank" class="hover:text-foreground transition-colors">LinkedIn</a>
                <span>09:17 Uhr MEZ</span>
              </div>

              <div class="flex flex-wrap gap-x-6 text-muted-foreground">
                <a href="/impressum" class="hover:text-foreground transition-colors">Impressum</a>
                <a href="/datenschutz" class="hover:text-foreground transition-colors">Datenschutz</a>
                <span>© 2026 Anika Warncke</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <AccessibilityPanel locale={ssrLocale} client:only="react" />
    <CookieBanner locale={ssrLocale} client:only="react" />

  </body>
</html>
